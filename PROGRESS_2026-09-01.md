# تقدم الشغل — 1 سبتمبر 2026 (+ متابعة 2 سبتمبر، 3 سبتمبر)

## ✅ تحديث 3 سبتمبر 2026 — الفعاليات (بند 1 من الـ Backlog): فحصتها كاملة لايف، لقيت وأصلحت باغ حقيقي

كل كود الفعاليات (باك اند + أدمن + الموقع العام) كان موجود من جلسة سابقة بس uncommitted وغير مفحوص لايف. فحصت الكل:

- **الباك اند**: `EventController` (public index/latest/show، contractor list/show/join/leave، admin CRUD + attendees) — فحصته بالكامل عبر curl/tinker (توكنات اختبار)، كل الـendpoints شغالة صح. الـmigrations (فصل الفعاليات عن الأخبار لجدول `events` مستقل، ترحيل بيانات، إعادة ربط `event_registrations`) اترانت محلياً من قبل وسليمة.
- **لوحة الأدمن** (`pages/events/index.vue`) — فحصتها لايف بالمتصفح: قائمة، فورم إنشاء/تعديل كامل (صورة رئيسية، معرض، فيديو، رابط خارجي، موعد/مكان/نوع حضور، بث مباشر، متحدثون repeater، نشر)، حذف — كلها شغالة 100%، الـ`POST`/`PUT`/`DELETE` كلها بترجع النتيجة الصح والجدول يتحدث.
- **🐛 باغ حقيقي لقيته وصلحته**: صفحة تفاصيل الفعالية على الموقع العام (`/landing/events/:slug`) ما كانت تشتغل إطلاقاً — الـURL بيتغير بس المحتوى يضل عالق على قائمة الفعاليات (نفس مشكلة `preview_click`/rAF إلي بالملاحظات القديمة تحت، بس هاي مختلفة تماماً وحقيقية بالتطبيق). السبب: `pages/landing/events.vue` (قائمة) وبنفس المستوى فولدر `pages/landing/events/[slug].vue` (تفاصيل) — بـ`unplugin-vue-router` وجود ملف وفولدر بنفس الاسم بيخلي الملف "أب" (layout) للراوتات جوا الفولدر، وبما إن `events.vue` ما فيه `<RouterView/>`، صفحة التفاصيل (`[slug].vue`) ما كانت تترندر أبداً — الـAPI call تبعها (`GET /api/v1/events/{slug}`) ما كان يصير إطلاقاً.
  - **الحل**: نقلت `pages/landing/events.vue` إلى `pages/landing/events/index.vue` (نفس المحتوى، بس مكانه تغيّر) — هيك صارت `events/index.vue` و`events/[slug].vue` أخوات جوا نفس الفولدر بدون تعارض اسم، فـما في nesting قسري. فحصتها لايف بعد التعديل (URL مباشر + كليك RouterLink طبيعي) — صفحة التفاصيل صارت تطلع صح بكل عناصرها (العنوان، التاريخ/المكان/نوع الحضور، المتحدثون، الفيديو، المعرض، إضافة للتقويم).
  - **⚠️ نفس الباغ موجود بالأخبار** (`pages/landing/news.vue` + `pages/landing/news/[slug].vue` نفس البنية) — ما صلحته (خارج نطاق "الفعاليات")، بس أنشأت task منفصلة له. لازم نفس الحل (نقل `news.vue` لـ`news/index.vue`).
- **فحصت التطبيق (contractor mobile API)**: تسجيل انضمام/إلغاء (`join`/`leave`)، `is_registered`/`attendees_count` بترجع صح بعد الفحص عبر توكن اختبار.
- **`npm run typecheck`**: 46 خطأ موجودين من قبل (مش من تعديلاتي — أغلبهم بخصوص نوع الـlayout `'landing'`/`'pure'` مو معرّف بنوع مشترك، موجودة بكل صفحات landing/contractor تقريباً؛ وأخطاء File type بصفحات تانية). ما زاد ولا نقص أي خطأ بتعديل نقل الملف (نفس الخطأ بس تغيّر مكانه من `landing/events.vue` لـ`landing/events/index.vue`).
- نظّفت بيانات الاختبار (event/registration مؤقتة) بعد الفحص — الداتا رجعت لنفس حالة الـseeder (فعالية FIDIC وحدة بس).

## ⚠️ الأهم: ولا شي متعمّل commit أو push لحد الآن
كل التعديلات (باك اند + فرونت) لسا **uncommitted** بالـ working tree. لازم مراجعة `git status` وعمل commit/push قبل ما يعتبر أي شي منها "منفّذ" فعلياً على السيرفر (InMotion live).

---

## ✅ تحديث 2 سبتمبر 2026 — أكملت الفجوتين المفتوحتين من آخر مرة

### 1. بطاقة ملخص الذمم (`.dues-summary-card`) — تأكدت، تشتغل صح
شغّلت الباك اند (`php artisan serve`) + الفرونت (`npm run dev`) + MariaDB (XAMPP)، سويت توكن اختبار لمقاول id=14، وفحصت لايف بالمتصفح. **الكود سليم 100%** — سبب "ما نقلت فعلياً" يوم أمس مو بالتطبيق، هو خلل بأداة المعاينة نفسها:
- `preview_click` أحياناً ما يسجّل الكليك فعلياً على العنصر (خلل بالأداة، مش بالتطبيق) — الحل: كليك يدوي عبر `dispatchEvent(new MouseEvent('click', ...))`.
- بعد أول كليك ناجح، الـ `<transition mode="out-in">` (`fade-slide`) بتعلق للأبد بحالة `leave-active` لأن **`requestAnimationFrame` ما بيشتغل إطلاقاً بهالبيئة headless** (تأكدت بتجربة مباشرة: rAF timeout بعد ثانيتين). Vue بيعتمد على rAF مضاعف لإكمال الـ transition، فبدونه العنصر القديم يضل عالق وما يطلع الجديد.
- **Workaround للفحص فقط** (مو لازم بالكود الحقيقي): حقن `<style>*{transition:none!important}</style>` + استبدال `window.requestAnimationFrame` بـ `setTimeout(cb,0)` قبل الكليك. بعدها البطاقة ظهرت صح: `الرصيد المستحق 1,560 د.أ`, `المدفوع 0`, `إجمالي الرسوم 1,560`, `2 غير مدفوع` — مطابق تماماً للـ API.
- **الخلاصة**: مستخدم حقيقي ببراوزر عادي (rAF شغال طبيعي) ما رح يواجه هالمشكلة. لا حاجة لأي تعديل بالكود.

### 2. الدعم الفني بلوحة المقاول (`pages/contractor/support.vue`) — بنيتها كاملة من الصفر
الباك اند كان جاهز أصلاً (`GET/POST contractor/support-tickets`, `GET contractor/support-tickets/{id}`, `POST contractor/support-tickets/{id}/reply`). أضفت بالفرونت:
- **قائمة تذاكري** — عرض subject/category/status/تاريخ، badge ملوّن حسب الحالة (مفتوحة/قيد المعالجة/تم الرد/مغلقة).
- **فورم تذكرة جديدة** — subject, whatsapp_phone (مسبوق برقم هاتف المقاول), category (select), message, مرفق اختياري (png/jpg/webp/pdf) — إرسال multipart/form-data.
- **تفاصيل التذكرة** — محادثة كاملة (فقاعات "أنت" يسار / "فريق الدعم" يمين)، عرض مرفقات، ردّ على التذكرة (يتقفل تلقائياً لو الحالة `closed`).
- **فحصتها لايف بالكامل**: إنشاء تذكرة جديدة (نجح، `201 Created`، ظهرت بأعلى القائمة بحالة "مفتوحة")، فتح تذكرة مغلقة قديمة (عرضت 4 رسائل بالتسلسل الصح، وما ظهر مربع الرد لأنها مغلقة)، إرسال رد على التذكرة الجديدة (ظهر بالمحادثة فوراً).
- `npm run typecheck` — ما زاد أي خطأ جديد (الخطأ الموجود بالسطر 8 بخصوص `layout: 'pure'` موجود أصلاً بكل صفحات المقاول، مش من هالتعديل).

### ملاحظة أداة جديدة لباكرة
لو رجعت تفحص تابات فيها `<transition>` بهالبيئة، تذكر مشكلة rAF فوق. ولو `preview_click` ما بيغيّر شي بالـ DOM رغم رسالة "Successfully clicked"، جرب `dispatchEvent` يدوي بدالها قبل ما تفترض في باغ حقيقي بالتطبيق.

### باقي الفجوات (نفس القايمة القديمة تحت "قيد التنفيذ")
كل شي لسا **uncommitted**. الفجوتين اللي كانوا "قيد التنفيذ"/"لسا ما بلشت" فوق خلصوا. باقي فقط الـ 8 كروت تحت "Trello Backend BackLog" + الملاحظات القديمة (باغ حذف مقاول عالسيرفر اللايف، cron entry، الخ) لسا كما هي.

---

## شو خلص (باك اند) — كله موثّق ومختبر بـ tinker

كل هاي من Trello "Backend BackLog":

- **البروفايل**: `verify-otp` صار يرجع name/authorized_person/membership_number. `GET contractor/auth/profile` صار يرجع `account_status` (بانر عضوية/ذمم/شهادات). تغيير رقم الجوال صار محمي بـ OTP (`phone/request-otp` + `phone/verify-otp`).
- **العطاءات**: فلاتر `created_from/created_to` + `bookmarked=1`.
- **الأخبار/الفعاليات**: `remove_gallery`، الصور تُضاف مش تُستبدل. حقول فعالية جديدة: `event_format`, `is_international`, `stream_url` (محمي بيوم الفعالية), `speakers[]`.
- **الذمم**: `financial()` صار يرجع `dues_total_jod`, `dues_paid_jod`, `dues_paid_percentage`, `dues_counts`, `pending_dues_payments`, `payment_history_counts`. `reference_number` جديد لكل ذمة. `myTransfers` فلاتر status/type.
- **البنوك/الدفع**: `bank-accounts` تجميع حسب البنك. `transaction_number` جديد لكل دفعة. `PaymentController::show()` جديد كامل.
- **سوق الآليات**: شغّلت migration ناقصة. `equipment/marketplace/{id}` جديد. إخفاء تلقائي عند انتهاء الاشتراك. `equipment/form-options` جديد. `subscription-status` صار يغطي الحالة المنتهية.
- **الشهادات**: SMS عند الإصدار.
- **عن الاتحاد/الإعدادات**: `about()` رؤية/رسالة/شارة/إحصائيات/خدمات + رفع غلاف. `general()` صار يرجع `exchange_rates` (سعر متوسط سلطة النقد، شغّلت `rates:fetch-pma` يدوياً).
- **الدعم الفني**: رجّعت مسارات الموبايل + أضفت كل مسارات الأدمن من الصفر + `whatsapp_phone`.

## شو خلص (فرونت — أدمن) — فحصته لايف بالبراوزر

1. **الدعم الفني** — صفحة `support-tickets.vue` استرجعتها (كانت محذوفة بكومنت `6f54eaa`)، أضفت عمود واتساب، رجّعت nav entries.
2. **عن الاتحاد** (`settings/app-settings.vue`) — رؤية/رسالة/شارة/إحصائيات/ريبيتر خدمات/رفع غلاف.
3. **فورم الأخبار/الفعاليات** (`news/index.vue`) — event_format/is_international/stream_url/speakers repeater + حذف صور گاليري موجودة.
4. **باقات الاشتراك** — صفحة جديدة كاملة `marketplace/packages.vue` (ما كانت موجودة أبداً) + nav entry.

## قيد التنفيذ الآن (فرونت — لوحة المقاول `pages/contractor/dashboard.vue`)

- ✅ **بانر account_status** — أضفته ومتحقق منه لايف (`.account-status-banner`, severity-success ظهر صح لمقاول عندو عضوية سارية).
- ✅ **بطاقة ملخص الذمم** (`.dues-summary-card` بتبويب "المعاملات المالية") — **تأكدت 2 سبتمبر: تشتغل صح**، التفاصيل بقسم "تحديث 2 سبتمبر" فوق.
- ✅ **الدعم الفني بلوحة المقاول** (`pages/contractor/support.vue`) — **خلصت 2 سبتمبر**: قائمة + تذكرة جديدة + تفاصيل/رد، التفاصيل بقسم "تحديث 2 سبتمبر" فوق.

## باقي (Trello Backend BackLog — 8 كروت لسا)

1. ~~انشاء API خاصة في الفعاليات~~ ✅ خلص 3 سبتمبر (باك اند + أدمن + عام) — تفاصيل فوق
2. API لعرض المكتبة القانونية
3. نصوص الخصوصية وساسية الاستخدام API
4. API عن التطبيق
5. شاشة التعميمات وتفاصيل التعميم API
6. انشاء API للتعميم الهام والعاجل
7. شاشة الاشعارات
8. إرسال sms للعضويات عند قرب انتهاء الاشتراك

## ملاحظات مهمة لباكرة

- **باغ حذف مقاول من لوحة الأدمن اللايف** — انصلح بالكود المحلي (`contractors/index.vue`: reset pagination + snackbar بعد حذف)، **بس ما انرفع عالسيرفر أبداً** — لسا ظاهر عند اليوزر بالـ live.
- **rates:fetch-pma** — الكود مجدول صح (`routes/console.php`)، بس محتاج cron entry يدوي على InMotion cPanel (كل دقيقة `php artisan schedule:run`) — ما قدرت أضيفه، ما عندي وصول.
- **preview_screenshot أداة معطّلة بهالبيئة** — استخدم `preview_snapshot`/`preview_eval` بدالها للتحقق.
- **توكنات اختبار مؤقتة** استخدمتها (منتهية الصلاحية ساعة وحدة، مش نافعة لباكرة) — لو محتاج تسجيل دخول مقاول للتجربة استخدم: `Contractor::find(ID)->createToken('test', ['*'], now()->addHour())->plainTextToken`.
- **بيانات دخول أدمن محلي**: `admin@union.ps` (كانت محفوظة بالمتصفح، مو من seeder — لو مش شغالة استخدم seeder: `admin@prometrica.com` / `Admin@2025!`).
