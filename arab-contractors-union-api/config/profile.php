<?php

return [

    /*
    |--------------------------------------------------------------------------
    | تعديلات ملف الشركة تمرّ بموافقة الإدارة (TASK-17 US11)
    |--------------------------------------------------------------------------
    |
    | مطفأ: السلوك هو سلوك ما قبل TASK-17 حرفياً — التطبيق يعدّل بيانات الشركة مباشرة.
    | مشتعل: الحقول المُراجَعة والمستندات تُحفَظ كطلب معلّق باللوحة، ولا تُطبَّق إلا بالموافقة.
    |
    | الافتراضي false عن قصد: يُتيح نزول الكود واختباره على staging بينما يبقى الإنتاج على
    | سلوكه الحالي، فلا يرتبط موعد نزوله بموعد تحديث تطبيق الموبايل. والتراجع إطفاء مفتاح
    | لا تراجُع عن migration.
    |
    | قبل تشعيله على الإنتاج: يجب أن يكون التطبيق يقرأ `pending_review` من الاستجابة، وإلا
    | أظهر للمقاول "تم الحفظ" على تعديل هو في الحقيقة قيد المراجعة.
    |
    */
    'edits_require_approval' => env('PROFILE_EDITS_REQUIRE_APPROVAL', false),

    /*
    | عدد الأيام التي تُحفظ بعدها الملفات المرحَّلة لطلبات لم تبقَ معلّقة، قبل أن يحذفها
    | profile-requests:prune-staged.
    */
    'staged_files_retention_days' => env('PROFILE_STAGED_RETENTION_DAYS', 30),

];
